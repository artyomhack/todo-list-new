spring:
  jpa:
    hibernate:
      ddl-auto: create
  application:
    name: todo-list

  datasource:
    url: jdbc:h2:file:~/data/test
    username: sa
    password: password
  h2:
    console:
      enabled: true
